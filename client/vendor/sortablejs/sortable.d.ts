// Vendored SortableJS (see README.md); typed at the surface the app uses.
declare const Sortable: {
  create(element: HTMLElement, options?: Record<string, unknown>): { destroy(): void };
};
export default Sortable;
